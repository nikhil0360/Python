

def disc(n):
	l1 = [d for d in str(n)]
	l2 = [d for d in str(n)]
	#print(l1)
	l3 = []

	#print(len(l1))
	for i in range(len(l1)):
		num_s = ""
		l1.pop(i)
		#print(l1)
		num_s= num_s.join(l1)
		num = int(num_s)
		l3.append(num)
		l1 = [d for d in str(n)]

	print(min(l3))	


l_in = []
nt = input()
for j in range(int(nt)) :
	a = input()
	l_in.append(a)


for k in l_in:
	disc(k)
